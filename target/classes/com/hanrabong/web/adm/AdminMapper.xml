<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.hanrabong.web.adm.AdminMapper">

	<insert id="insertAdmin" parameterType="Admin">
	
	</insert>
	
	<select id="selectAdminByIdpw" parameterType="Admin" resultType="Admin">
	SELECT A.* FROM HADMIN A WHERE AID = #{aid}
	</select>

</mapper>