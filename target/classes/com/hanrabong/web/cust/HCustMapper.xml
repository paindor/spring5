<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.hanrabong.web.cust.HCustMapper">

	<select id="selectCustById" resultType="cust" >
		SELECT CNUM, CID , CPW , CNAME FROM HCUST WHERE CID LIKE #{cid} 
			AND CPW LIKE #{cpw}
		
	</select>
	<insert id="insertUser" parameterType="cust" >
	   INSERT INTO HCUST(CNUM , CID, CPW)
		VALUES( #{cnum} ,#{cid}, #{cpw})
						
	</insert>

	<select id="existId" parameterType="String" resultType="int">
	SELECT COUNT(CID) FROM HCUST WHERE CID LIKE #{cid}
	
	</select>
	

</mapper>